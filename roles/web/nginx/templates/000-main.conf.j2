server_tokens off;

access_log off;

log_format vhosts '$remote_addr - $remote_user [$time_local] "$request_method $scheme://$host$request_uri $server_protocol" $status $body_bytes_sent "$http_referer" "$http_user_agent" $request_time $upstream_response_time $pipe';

access_log /var/log/nginx/access.log vhosts;
error_log /var/log/nginx/error.log;

allow   127.0.0.1;
allow   {{ wsl_private_ip }};
allow   {{ wsl_windows_ip }};
deny    all;

